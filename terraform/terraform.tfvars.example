# ⚠️ NOTA: Este es solo un archivo de EJEMPLO para referencia
# 📁 El archivo real de configuración es terraform/terraform.tfvars
# 🚫 NO modifiques este archivo - es solo para documentación

# Ejemplo de configuración completa:

aws_region    = "us-east-1"
project_name  = "helipistas-odoo"
environment   = "production"

vpc_cidr           = "10.0.0.0/16"
public_subnet_cidr = "10.0.1.0/24"

instance_type      = "t3.medium"
spot_price        = "0.05"
root_volume_size  = 30

key_pair_name = "mi-key-pair-aws"  # Nombre de tu Key Pair en AWS (sin .pem)

allowed_ssh_cidr = "0.0.0.0/0"

odoo_master_password = "[CAMBIAR_POR_CONTRASEÑA_SEGURA]"
postgres_password    = "[CAMBIAR_POR_CONTRASEÑA_SEGURA]"

domain_name       = "odoo.tudominio.com"  # Opcional
letsencrypt_email = "admin@tudominio.com" # Opcional

existing_elastic_ip_id = "eip-1234567890abcdef0"  # Opcional
existing_efs_id = "fs-1234567890abcdef0"          # Opcional
efs_mount_point = "/efs"
